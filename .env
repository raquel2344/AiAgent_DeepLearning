# -------- OpenAI API --------
OPENAI_API_KEY=sk-your-openai-key

# -------- Google API Credentials --------
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REFRESH_TOKEN=your-google-refresh-token
GOOGLE_PROJECT_ID=your-google-cloud-project-id
GOOGLE_CALENDAR_ID=primary

# -------- OAuth2 Redirect & Scopes --------
GOOGLE_REDIRECT_URI=http://localhost:8080
GOOGLE_SCOPES=https://www.googleapis.com/auth/calendar,https://www.googleapis.com/auth/contacts

# -------- Server Settings (if using Flask or similar) --------
HOST=127.0.0.1
PORT=5000
DEBUG=True

# -------- Memory or DB (if expanded) --------
MEMORY_MODE=local
MEMORY_BACKEND=json

# -------- Security / Validation --------
ENABLE_SAFETY_CHECKS=true

# -------- Optional Logging --------
LOG_LEVEL=INFO


To use this file:

1. Save it as .env in the root of your project.

2. Load it in Python using python-dotenv like this:

from dotenv import load_dotenv
load_dotenv()
